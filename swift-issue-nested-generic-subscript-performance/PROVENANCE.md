# Provenance

The manifest, benchmark, library source, and `SIL.md` are exact blobs from the public source repository at the packet-pinned `main` commit recorded in `evidence/source-provenance.json`. No ancestry is imported and no fresh result is claimed.

