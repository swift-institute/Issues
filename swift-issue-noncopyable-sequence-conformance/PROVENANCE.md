# Provenance

The manifest and four source files are exact blobs from the public source repository at the packet-pinned `main` commit recorded in `evidence/source-provenance.json`. No ancestry is imported and no fresh compiler result is claimed.

