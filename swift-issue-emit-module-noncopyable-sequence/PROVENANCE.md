# Provenance

The retained package files are exact blobs from the public source repository and packet-pinned `main` commit identified in `evidence/source-provenance.json`. The former `coenttb` coordinate was transferred to the current transitional `swift-institute` source; neither coordinate is the canonical work item.

No Git ancestry is imported. This staging receipt covers retained blobs and visibility only; it is not the complete archive/recovery receipt required for source deletion.

