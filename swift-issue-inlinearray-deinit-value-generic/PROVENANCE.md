# Provenance

The manifest, library source, and test are exact blobs from the public source repository at the packet-pinned `main` commit in `evidence/source-provenance.json`. No ancestry is imported, and no fresh execution result is claimed.

