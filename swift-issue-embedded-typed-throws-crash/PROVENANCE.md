# Provenance

The forensic resources are exact blobs from the public source repository at the packet-pinned `main` commit in `evidence/source-provenance.json`. The `.txt` suffix is a staging safety measure and does not alter the bytes.

No ancestry is imported. This is not a deletion or cold-archive receipt.

