# Provenance

The forensic manifest and source are exact blobs from the public repository’s packet-pinned `main` commit. The `.txt` suffix changes only the retained filename, not its bytes. See `evidence/source-provenance.json`.

