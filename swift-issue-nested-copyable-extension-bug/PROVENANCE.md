# Provenance

The forensic resources are exact blobs from the public source repository at the packet-pinned `main` commit. The `.txt` suffixes change only retained filenames. See `evidence/source-provenance.json`.

This dossier deliberately does not overstate the incomplete standalone reduction as a reproducer.

